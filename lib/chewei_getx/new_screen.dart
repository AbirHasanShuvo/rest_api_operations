import 'package:flutter/material.dart';

class NewScreenGetx extends StatelessWidget {
  const NewScreenGetx({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("This is new screen"),),
    );
  }
}
